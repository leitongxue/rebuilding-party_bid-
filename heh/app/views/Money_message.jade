header
      {{title}}（{{number}}人）


      .header-left
         button.btn.btn-default.btn-lg(ng-click="back()") 返 回
      .header-right
         button.btn.btn-default.btn-lg(ng-click="end()")(ng-disabled="xian") 结 束

div#wrapper(class="wrapper scrollable")
    ul(class="list-style-2")
        li(ng-repeat="messages in middle track by $index")
            h3 {{messages.name}}
            h3.header-right{{messages.phone}}